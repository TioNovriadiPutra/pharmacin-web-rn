import usePekerjaanModel from "@models/pekerjaanModel";

const usePekerjaanController = () => {
  const { useGetOccupations } = usePekerjaanModel();

  const useGetOcuppationsQuery = () => {};
};

export default usePekerjaanController;
